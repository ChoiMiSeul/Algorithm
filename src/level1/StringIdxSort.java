package level1;

import java.util.Comparator;

public class StringIdxSort implements Comparator<String>{
	/**
	 * 프로그래머스 Level 1 문자열 내 마음대로 정렬
	 * @param strings
	 * @param n
	 * @return
	 */
	public String[] solution(String[] strings, int n) {
		//1. comparator 이용해 조건 정렬
		String[] answer= {};
		Arrays.sort(strings, new Comparator());
		return answer;
	}

}